# config/app.yml for rails-settings-cached
defaults: &defaults
  tracker_name: "TheCulture.eu"
  tracker_url: "http://localhost:3000"

development:
  <<: *defaults

test:
  <<: *defaults

production:
  <<: *defaults
